- maintenance
  - playlist reorganization
  - DB schema versioning (auto clean if no match)
  - show confirm dialog on page refresh if player.playing
  - rotate log files / show only last file with API

- new feature
  + metadata edition
  + i18n
  - SSL launcher
  - random playback
  - android application
  + search
  + volume
  - mono playback
  + first time login: force new password, directories
  - performance test with curl
  + welcome page (do not default to /?artists/)
